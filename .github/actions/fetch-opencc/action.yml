name: Fetch verified OpenCC data
description: Download the immutable OpenCC asset lock and verify its checksum.

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        pixi run python tools/fetch_opencc.py \
          --lock tools/opencc.lock.json \
          --destination opencc/eosphoros
