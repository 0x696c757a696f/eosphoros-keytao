name: Set up repository Pixi environment
description: Install Pixi, restore its cache, and materialize the locked environment.

runs:
  using: composite
  steps:
    - uses: prefix-dev/setup-pixi@v0.10.0
      with:
        cache: true
        run-install: true
