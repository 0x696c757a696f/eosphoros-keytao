# 为晨星键道贡献

请优先使用仓库的问题模板。词条反馈应写明词条、当前编码、期望编码、读音及
使用场景；涉及多音字时必须指出实际读音。宗教专词还应提供可公开访问的权威
来源，但不要复制词典释义或受版权保护的正文。

## 本地检查

推荐使用 Pixi，仓库不依赖维护者电脑上的绝对路径：

```powershell
pixi run check
```

只检查较快的静态质量规则时可运行：

```powershell
pixi run quality
```

仓库文本文件统一使用 LF。若编辑器或生成器写入了 CRLF，可运行：

```powershell
pixi run line-endings
```

## 词库原则

- 不直接编辑自动生成的 ICE、万象、英文及四个非天主教专题词典；修改其清单、
  筛选器或生成器。
- 不处理单字表内容，除非问题来自明确的上游同步错误。
- 不因字数长就删除药名、正式机构、行政区、文献或人物全名。
- 不加入能够自然分开输入的句子、口号、聊天回复或机械拼接的限定短语。
- 不为塞入词条而自创飞键；没有空闲合法码时允许跳过。

提交前必须保证 `git diff --check`、全部测试、生成一致性和仓库验证通过。
